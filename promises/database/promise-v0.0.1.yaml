# A snapshot of promise.yaml as it looked at v0.0.1, before the
# highAvailability field was added at v0.2.0 (see promise.yaml itself).
# `promise-demo` installs this first, then promise.yaml on top of it, so a
# fresh `make up` gets a real, historical second PromiseRevision instead of
# a synthesized one - the same mechanism a real Promise upgrade uses. See
# docs/superpowers/specs/2026-08-14-promise-version-upgrades-design.md.
apiVersion: platform.kratix.io/v1alpha1
kind: Promise
metadata:
  creationTimestamp: null
  labels:
    kratix.io/promise-version: v0.0.1
    marketplace.kratix.io/visible: "true"
  annotations:
    marketplace.kratix.io/display-name: Postgres Database
    marketplace.kratix.io/description: A sized, managed Postgres database, provisioned on request.
    marketplace.kratix.io/owner: platform-team
    marketplace.kratix.io/lifecycle: stable
    marketplace.kratix.io/support: "#platform-eng"
    marketplace.kratix.io/policy: confidential
  name: database
spec:
  api:
    apiVersion: apiextensions.k8s.io/v1
    kind: CustomResourceDefinition
    metadata:
      creationTimestamp: null
      name: databases.demo.kratix.io
    spec:
      group: demo.kratix.io
      names:
        kind: Database
        plural: databases
        singular: database
      scope: Namespaced
      versions:
      - name: v1alpha1
        schema:
          openAPIV3Schema:
            properties:
              spec:
                properties:
                  size:
                    type: string
                    enum:
                    - 1Gi
                    - 5Gi
                    - 10Gi
                    - 50Gi
                    description: Storage volume size for the database.
                required:
                - size
                type: object
            type: object
        served: true
        storage: true
    status:
      acceptedNames:
        kind: ""
        plural: ""
      conditions: null
      storedVersions: null
  workflows:
    config: {}
    promise:
      configure:
      - apiVersion: platform.kratix.io/v1alpha1
        kind: Pipeline
        metadata:
          name: dependencies
        spec:
          containers:
          - image: kratix-guide/database-promise-pipeline:v0.1.0
            name: configure-deps
    resource:
      configure:
      - apiVersion: platform.kratix.io/v1alpha1
        kind: Pipeline
        metadata:
          name: database-configure
        spec:
          containers:
          - image: kratix-guide/database-resource-pipeline:v0.1.0
            name: kratix-guide-database-resource-pipeline
status:
  workflows: 0
  workflowsFailed: 0
  workflowsSucceeded: 0
